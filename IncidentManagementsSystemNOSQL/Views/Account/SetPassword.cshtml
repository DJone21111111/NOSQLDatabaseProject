@model IncidentManagementsSystemNOSQL.Models.SetPasswordViewModel

@{
    Layout = null;
    ViewData["Title"] = "Set Your Password";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Set Password - The Garden Group</title>
    <link rel="stylesheet" href="~/css/login.css" asp-append-version="true" />
    <style>
        .error-message {
            color: #c0392b; /* deep red */
            font-weight: 600;
            margin-bottom: 1rem;
            text-align: center;
            background: rgba(255, 0, 0, 0.05);
            border: 1px solid #f5b7b1;
            border-radius: 6px;
            padding: 0.6rem 1rem;
        }
    </style>
</head>
<body>
    <div class="wrap">
        <div class="card">
            <div class="brand">
                <img src="~/Images/flower.png" asp-append-version="true" alt="The Garden Group" class="logo" />
                <div class="company">The Garden Group</div>
                <div class="system-badge">Incident Management System</div>
                <h1>Set Your Password</h1>
                <p style="font-size: 0.9rem; color: #555;">Enter your assigned username and choose a secure password.</p>
            </div>

            <form asp-action="SetPassword" method="post" novalidate>
                @Html.AntiForgeryToken()

                @if (!ViewData.ModelState.IsValid)
                {
                    <div class="error-message">
                        @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                        {
                            <div>@error.ErrorMessage</div>
                        }
                    </div>
                }

                <label asp-for="Username">Username</label>
                <input asp-for="Username" class="field" placeholder="Enter your assigned username" />

                <label asp-for="Password">New Password</label>
                <input asp-for="Password" type="password" class="field" placeholder="Enter new password" />

                <label asp-for="ConfirmPassword">Confirm Password</label>
                <input asp-for="ConfirmPassword" type="password" class="field" placeholder="Confirm new password" />

                <button type="submit" class="btn">Set Password</button>

                <div class="links">
                    <a asp-controller="Account" asp-action="Login">Back to Login</a>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
